package TwoWheeler


sequence Strategy {
	when (module.id == @Car.Driver && module.proximity() > PROXIMITY) {
		execute Turn(LEFT); 
	} 
	meta.drive(FORWARD,FORWARD); 
} 
Instruction start 0:Wait(FieldPrimRef(Config,"assignTime"))